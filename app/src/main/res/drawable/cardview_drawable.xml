<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sombra -->
    <item
        android:left="2dp"
        android:top="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#33000000" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <!-- Fondo con gradiente -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="#FFFFFF"
                android:endColor="#F5F5F5"
                android:angle="90" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <!-- Borde decorativo -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="4dp"
                android:color="#FF5252" /> <!-- Rojo vibrante -->
            <corners android:radius="16dp" />
        </shape>
    </item>

    <!-- Detalle esquina superior izquierda -->
    <item
        android:top="8dp"
        android:left="8dp"
        android:right="8dp"
        android:bottom="8dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="1dp"
                android:color="#FFCDD2" /> <!-- Rojo claro -->
            <corners android:radius="12dp" />
        </shape>
    </item>

</layer-list>